<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Aga Esports | Official</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Orbitron:wght@400;700&family=Rajdhani:wght@500&display=swap" rel="stylesheet">
    
    <style>
        :root { --neon-blue: #00f2ff; --dark-bg: #0a0b10; --card-bg: #161b22; }
        body { background-color: var(--dark-bg); color: #e6edf3; font-family: 'Rajdhani', sans-serif; overflow-x: hidden; }
        h1, h2, h3 { font-family: 'Orbitron', sans-serif; text-transform: uppercase; }
        
        .neon-text { color: var(--neon-blue); text-shadow: 0 0 10px rgba(0, 242, 255, 0.7); }
        .navbar { background: rgba(10, 11, 16, 0.95) !important; border-bottom: 2px solid var(--neon-blue); }
        .hero-section { padding: 100px 0; background: linear-gradient(rgba(0,0,0,0.7), rgba(10,11,16,1)), url('https://source.unsplash.com/random/1600x900/?gaming'); background-size: cover; }
        
        .section-title { margin-bottom: 50px; position: relative; display: inline-block; }
        .section-title::after { content: ''; position: absolute; width: 50%; height: 3px; background: var(--neon-blue); bottom: -10px; left: 25%; }

        .player-card { background: var(--card-bg); border: 1px solid #30363d; border-radius: 15px; transition: 0.4s; overflow: hidden; }
        .player-card:hover { transform: scale(1.05); border-color: var(--neon-blue); box-shadow: 0 0 20px rgba(0, 242, 255, 0.3); }
        .player-img { width: 100%; height: 250px; object-fit: cover; }

        .result-box { border-radius: 10px; padding: 20px; margin-bottom: 20px; transition: 0.3s; background: rgba(255,255,255,0.05); }
        .win { border-left: 5px solid #2ecc71; }
        .lose { border-left: 5px solid #e74c3c; }
        
        .footer { padding: 40px 0; border-top: 1px solid #30363d; margin-top: 100px; text-align: center; }
    </style>
</head>
<body>

    <% if(music && music.url) { %>
        <div style="display:none">
            <audio id="bgMusic" autoplay loop>
                <source src="<%= music.url %>" type="audio/mpeg">
            </audio>
        </div>
        <script>
            // Tarayıcıların autoplay engelini aşmak için ufak bir dokunuş
            document.body.addEventListener('click', () => {
                document.getElementById('bgMusic').play();
            }, { once: true });
        </script>
    <% } %>

    <nav class="navbar navbar-expand-lg navbar-dark sticky-top">
        <div class="container">
            <a class="navbar-brand neon-text fw-bold" href="#">AGA ESPORTS</a>
            <div class="navbar-nav ms-auto">
                <a class="nav-link" href="#about">Hakkımızda</a>
                <a class="nav-link" href="#roster">Roster</a>
                <a class="nav-link" href="#results">Maçlar</a>
                <a class="nav-link btn btn-outline-info ms-lg-3" href="/login">Admin</a>
            </div>
        </div>
    </nav>

    <div class="hero-section text-center">
        <h1 class="display-2 fw-bold neon-text">Zirve Bizim</h1>
        <p class="lead">Espor Dünyasında Yeni Bir Çağ Başlıyor.</p>
    </div>

    <div class="container">
        <section id="about" class="py-5 text-center">
            <h2 class="section-title neon-text">Hakkımızda</h2>
            <p class="fs-5 mx-auto" style="max-width: 800px;">
                Aga Esports olarak, profesyonel oyun dünyasında rekabetçi ruhu ve takım oyununu en üst seviyeye taşımayı hedefliyoruz. 
                Sadece kazanmak için değil, espor kültürünü büyütmek için buradayız.
            </p>
        </section>

        <section id="roster" class="py-5">
            <h2 class="text-center section-title neon-text">Oyuncularımız</h2>
            <div class="row g-4 mt-2">
                <% if(roster && roster.length > 0) { %>
                    <% roster.forEach(player => { %>
                    <div class="col-md-3">
                        <div class="player-card text-center">
                            <img src="<%= player.image %>" class="player-img" alt="Oyuncu">
                            <div class="p-3">
                                <h4 class="mb-0 text-white"><%= player.nick %></h4>
                                <small class="text-info"><%= player.role %></small>
                            </div>
                        </div>
                    </div>
                    <% }) %>
                <% } else { %>
                    <p class="text-center">Henüz oyuncu eklenmedi. Admin panelinden ekle!</p>
                <% } %>
            </div>
        </section>

        <section id="results" class="py-5">
            <h2 class="text-center section-title neon-text">Maç Sonuçları</h2>
            <div class="row mt-4">
                <div class="col-md-6">
                    <h3 class="text-success mb-4">Galibiyetler (WINS)</h3>
                    <% if(gallery && gallery.wins.length > 0) { %>
                        <% gallery.wins.forEach(img => { %>
                            <div class="result-box win">
                                <img src="<%= img %>" class="img-fluid rounded" alt="Win">
                            </div>
                        <% }) %>
                    <% } %>
                </div>
                <div class="col-md-6">
                    <h3 class="text-danger mb-4">Mağlubiyetler (LOSSES)</h3>
                    <% if(gallery && gallery.loses.length > 0) { %>
                        <% gallery.loses.forEach(img => { %>
                            <div class="result-box lose">
                                <img src="<%= img %>" class="img-fluid rounded" alt="Lose">
                            </div>
                        <% }) %>
                    <% } %>
                </div>
            </div>
        </section>
    </div>

    <footer class="footer">
        <div class="container">
            <h2 class="neon-text mb-4">İLETİŞİM</h2>
            <p>Sponsorluk ve iş birliği için: <br> <strong>iletisim@agaesports.com</strong></p>
            <div class="mt-4">
                <small>© 2026 Aga Esports. Tüm Hakları Saklıdır.</small>
            </div>
        </div>
    </footer>

</body>
</html>
